# ============================================================
# Credentials Jenkins
# ============================================================
credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "git-credentials"
              description: "Credentials Git"
              username: "${GIT_USERNAME:-Herlymba828}"
              password: "${GIT_TOKEN:-}"
